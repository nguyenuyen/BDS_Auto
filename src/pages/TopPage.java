import org.openqa.selenium.WebDriver;

public class TopPage {
    WebDriver webDriver;

    public TopPage(WebDriver webDriver) {
        this.webDriver = webDriver;
    }

//    public boolean isDisplayed() {
//        if (Element.URL_DASHBOARD.equals(webDriver.getCurrentUrl())) {
//            return true;
//        } else {
//            return false;
//        }
//    }
}
